defaults:
  - MODELS@model: qwen2.5
  - TRAINERS@trainer: GRPO
  - _self_

dataset:
  train_path: DATA/FOLIO/folio_train.jsonl
  validation_path: DATA/FOLIO/folio_validation.jsonl
  test_path: DATA/FOLIO/folio_test.jsonl

experiment:
  name: logical-feedback-training
  description: General trainer entrypoint with model-specific configuration.

hydra:
  run:
    dir: ${trainer.output_dir}/${now:%Y-%m-%d}/${now:%H-%M-%S}
